<p id="notice"><%= notice %></p>

<div class="ShadowBox">

<center><h1 class= "section-heading" style="color:green;font-size:300%;font-family:'Contrail One',cursive; box-shadow: 0 1px 1px 0 rgba(0, 0, 0, 0.12);">Boat Shipments</h1>

<table>
  <thead>
    <tr>
      <th>Boat</th>
      <th>Shipments</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @boat_jobs.each do |boat_job| %>
      <tr>
        <td><%= boat_job.boat_id %></td>
        <td><%= boat_job.job_id %></td>
        <td><%= link_to 'Show', boat_job %></td>
        <td><%= link_to 'Edit', edit_boat_job_path(boat_job) %></td>
        <td><%= link_to 'Destroy', boat_job, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Boat Job', new_boat_job_path, class: "btn btn-primary btn-xl text-uppercase", style: "color:white" %>


</center>
</div>
