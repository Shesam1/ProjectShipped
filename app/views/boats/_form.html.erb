<%= form_with(model: boat, local: true) do |form| %>
  <% if boat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(boat.errors.count, "error") %> prohibited this boat from being saved:</h2>

      <ul>
      <% boat.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :boat_name %>
  </div>

<br>
  <div class="field">
    <%= form.label :size %>
    <%=form.select :size , options_for_select([
    ['Small (500 Max)'], ['Medium (1000 Max)'],['large(2000 Max)' ]] )%>
  </div>

<br>
  <div class="field">
    <%= form.label :image %>
    <%= form.file_field :image, id: :boat_photo %>
  </div>

<br>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
